<main>

  <div>
  <div>
  <form action='/reviews/create' method='GET'>
    <button type='submit' id='createButton'>Create Review</button>
  </form>
  </div>

  <!-- FILTER -->
  <div class='filter'>
    <form class='reviewFilter' method="GET">
      <h2>Filter By:</h2>
      <div>
      <label for='dateFilter'>Date
      <input type='checkbox' class='dateFilter'/>
      </label>
      </div>
      <div>
      <label for='userFilter'>Username
      <input type='checkbox' class='userFilter'/>
      </label>
      </div>
      <button type='submit' class='filterButton'>Filter</button>
    </form>
  </div>
</div>

  <!-- ALL REVIEWS -->
  <div class='reviewBody'>
    <h1>Reviews</h1>
    <hr>
    {{#each review}}
      <a href='/trending'>
        <h2>{{this.title}}</h2>
        <p>By: {{this.user}}</p>
        <br>
        <p>{{this.gameName}}</p>
        <p>{{this.postContent}}</p>
        <p>{{this.datePublished}}</p>
        <hr>
      </a>
    {{/each}}
  </div>

</main>
